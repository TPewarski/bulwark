import equityResolvers from "./assets/equityResolvers";
import portfolioResolvers from "./portfolio/portfolioResolvers";

const resolvers = {
  ...equityResolvers,
  ...portfolioResolvers,
};

export default resolvers;
